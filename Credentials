pipeline {
    agent any
    stages {
        stage('Access Secret Text') {
            steps {
                withCredentials([string(credentialsId: 'your-secret-id', variable: 'SECRET_TEXT')]) {
                    script {
                        echo "Using secret: ${SECRET_TEXT}"
                    }
                }
            }
        }
    }
}
